# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod Properties
mod_version=1.0.4
maven_group=online.slavok
archives_base_name=simple-whitelist

# Modrinth project (slug or id) — public, used by the release pipeline.
modrinth_id=JirFBOYr

# Shared, version-independent dependency versions.
# Per-Minecraft-version deps (yarn, fabric-api, fabric-language-kotlin,
# fabric-permissions-api, java level) live in the build.gradle.kts matrix.
loader_version=0.18.1
kotlinx_serialization_version=1.7.1
mysql_connector_version=9.0.0
