org.gradle.jvmargs=-Xmx2G
plugin_version=1.0.4

# Modrinth project (slug or id) — same project as the mod; the release pipeline
# publishes the plugin jar here under a distinct version string.
modrinth_id=JirFBOYr
